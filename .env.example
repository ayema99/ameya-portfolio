# Groq API key — get a free key at https://console.groq.com
# Add this to Vercel project settings → Environment Variables
# For local dev with chat: copy to .env.local and run `vercel dev`
GROQ_API_KEY=
